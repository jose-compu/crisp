-- Shape bound on a concrete type resolves (#61).
shape HasPosition = {
    x: float
    y: float
}

type Point = {
    x: float
    y: float
}

distance(a: Point + shape HasPosition) = 0.0

pub main() = {
    log("unused")
}
