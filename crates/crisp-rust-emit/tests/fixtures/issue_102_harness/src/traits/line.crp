trait Show = {
    show(self) -> str
}

pub type Line = {
    text: str
}

impl Show for Line = {
    show(self) = self.text
}

test "show line" = {
    v := Line {
        text: "ok"
    }
    assert_eq(v.show(), "ok")
}
