[package]
name = "net_http"
version = "0.1.0"
edition = "2026"

[build]
target = "rust"
runtime = "tokio"
error_model = "ambient"

[dependencies]
# Thin HTTP re-export (§15.2 / #28) — same rust=true path as serde_json (#41).
ureq = { rust = true, version = "2" }
